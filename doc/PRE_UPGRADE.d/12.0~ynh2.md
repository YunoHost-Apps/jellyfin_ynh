This upgrade moves the cache and transcoding cache directories to the app's data directory, so that they will not uselessly be backed up during upgrades anymore. To speed up the process, you should manually clear them before proceeding: from Jellyfin's dashboard, go to `Planned tasks` and hit the `Empty cache directory` button.
